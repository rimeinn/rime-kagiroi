schema: kagiroi_flick
source_dir: ..
deploy:
  default:
    tests:
      - send: a
        assert: cand[1].text == "あ"
      - send: aX
        assert: cand[1].text == "ぁ"
      - send: aXX
        assert: cand[1].text == "あ"
    
  nocrash:
    tests:
      - send: nani{space}
        assert: true
      - send: nani{space}
        assert: true
